{
  "name": "licenses-list-generator",
  "version": "0.7.2",
  "description": "Fetch licences types and texts from all dependencies (and their dependencies) from project.",
  "main": "src/index.js",
  "scripts": {
    "licenses": "node build/licenses-generator",
    "lint": "eslint src/*.js --fix",
    "test": "npm run lint && npm run unit",
    "unit": "jest --config jest.config.json"
  },
  "dependencies": {
    "chalk": "^4.1.0"
  },
  "devDependencies": {
    "eslint": "^7.10.0",
    "jest": "^26.4.2"
  },
  "engines": {
    "node": ">=13.2.0",
    "npm": ">=6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jacekwasowski/licenses-list-generator.git"
  },
  "keywords": [
    "license",
    "licenses",
    "legal",
    "OSS"
  ],
  "author": "Jacek Wasowski",
  "license": "MIT"
}
